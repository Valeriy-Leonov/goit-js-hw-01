let name = String("whatever");
let price = 1000000;

const firstMessage = `Choised "${name}", price per one unite is ${price} credits`;

console.log(firstMessage.split(" ", 1));

price = 2000000;

const secondMessage = `Choised "${name}", price per one unite is ${price} credits`;

console.log(secondMessage);
